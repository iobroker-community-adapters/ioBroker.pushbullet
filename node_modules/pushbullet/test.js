var PushBullet = require('./index.js');

var pusher      = new PushBullet('2970084453a90fbc5bb521f5192683a1');
//var pusher      = new PushBullet('test');
var gs2Iden     = 'udhrSddkxW';
var firefoxIden = 'udhrSdjzWIEVDzOK';

function callback(err, response) {
	if (err) {
		console.dir(err);
		return;
	}
	console.log(JSON.stringify(response, null, 4));
}

//pusher.me(callback);

pusher.file(firefoxIden, '/home/alex/projects/node-pushbullet-api/README.md', 'README', callback);

//pusher.file(gs2Iden, __dirname + '/README.md', 'Test', callback);

/*pusher.devices(callback);*/


//pusher.note({ device_iden: firefoxIden, source_device_iden: gs2Iden }, 'Test note', 'Note body', callback);

//pusher.channelInfo('jblow', callback);

//pusher.subscriptions(callback);

//pusher.list(gs2Iden, 'List', ['One', 'Two', 'Three'], callback);

/*var updates = {
	items: [
		{
			text: 'One'
		},
		{
			text: 'Two'
		},
		{
			text: 'Three',
			checked: true
		}
	]
};

pusher.updatePush('udhrSsjz0Z1Zjaea', updates, callback);*/

//pusher.history({ modified_after: 1412427860 }, callback);

// pusher.deletePush('udhrSsjz0Z1Zjaea', callback);
